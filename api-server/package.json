{
  "name": "api-server",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write src/**/*.ts"
  },
  "author": "Andrew Kaiser",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
